<?php 

/*
* ---------------------------------------------------------
* Register the auto loader
* ---------------------------------------------------------
*/
require __DIR__ . '/vendor/autoload.php';

use Quantum\Console\QtConsole;

/*
* ---------------------------------------------------------
* Run the console application
* ---------------------------------------------------------
*/
$console = new QtConsole();

$status = $console->init(__DIR__);

exit($status);
